@model JiaoZi.Models.shouyeban
<style>
</style>
@*<h3 style="font-style: italic;">TOP NEWS </h3>
<div id="myCarousel" class="carousel slide text-center " data-ride="carousel">
    <ol class="carousel-indicators">
        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
        <li data-target="#myCarousel" data-slide-to="1"></li>
        <li data-target="#myCarousel" data-slide-to="2"></li>
        <li data-target="#myCarousel" data-slide-to="3"></li>
        <li data-target="#myCarousel" data-slide-to="4"></li>
        <li data-target="#myCarousel" data-slide-to="5"></li>
    </ol>
    <!-- Indicators -->
    <div class="carousel-inner" role="listbox">
        @{ int i = 0;}
        @foreach (var item in Model.newstops)
        {
            <span style="opacity:0"> @(i++)</span>
            if (i == 1)
            {
                <div class="item active"style="height:100px;">
                    <h1>@item.Title<br><span style="font-style:normal;">@item.News_Time</span></h1>
                </div>
            }
            else
            {
                <div class="item"style="height:100px;">
                    <h1>@item.Title<br><span style="font-style:normal;">@item.News_Time</span></h1>
                </div>
            }
        }
    </div>
    <!-- Left and right controls -->
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>*@
<div class="col-md-1"><h1>最<br />新<br />资<br />讯</h1></div>
<div class="col-md-1" style="height:253px;width:10px;border-left:5px #ff0000 solid"></div>
<div class="col-md-10 ">
    <ul>
        @{ int i = 1;}
        @foreach (var item in Model.newstops)
        {
          
            <li>
                @item.News_Time &nbsp;
                <span class="label label-danger">@(i++)&nbsp;</span>
                <a href="@Url.Action("Newsdetails","News",new { newsid=item.NewsID})">@item.Title</a>
                @*<a href="@Url.Action(" Index","New")" style="font-size:6px"></a>*@
                <br />
            </li>
            <li>
                <br />
            </li>
        }
    </ul>
</div>